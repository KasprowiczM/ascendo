# Landing UI Kit

Recreation of the Ascendo marketing site (`website/` in the source repo). Astro in production; this kit is plain HTML/CSS as a static visual reference.

Open `index.html` to see the full above-fold composition.
