# Desktop App UI Kit

The Tauri shell wraps the same FastAPI backend as the CLI and renders the same SPA. Same surface as the web app, framed in OS chrome — system tray, focus-aware notifications, and a global keyboard shortcut to open the run center.

This view shows the macOS variant. Windows and Linux versions use native chrome but identical content.
