"""Tests for adapters/windows/ — pytest + Pester.

- Python tests (pytest) for ascendo_windows/* — mocked subprocess
- PowerShell tests (Pester 5.x) for scripts/ + lib/*.ps1
- Golden test fixtures with UTF-8 ellipsis edge cases (regression for column-parser bug)

Contract tests live in tests/contract/ at repo root.
"""
