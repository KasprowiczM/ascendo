"""Tests for adapters/ubuntu/ — pytest + bats.

- Python tests (pytest) for ascendo_ubuntu/* — mocked subprocess
- Bash tests (bats) for scripts/ + lib/* — D:\Dev_Env\Ubuntu_Aktualizacje\tests\bash\

Contract tests live in tests/contract/ at repo root and apply to all adapters.
"""
