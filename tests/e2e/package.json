{
  "name": "ascendo-e2e",
  "private": true,
  "description": "Headless Playwright smoke tests for the Ascendo dashboard SPA (dev-only; not shipped).",
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "scripts": {
    "test": "playwright test"
  }
}
