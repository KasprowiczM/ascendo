#!/usr/bin/env bash
# ascendo_restart_web — stop then start the FastAPI dashboard.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
"$SCRIPT_DIR/ascendo_stop_web" || true
sleep 1
exec "$SCRIPT_DIR/ascendo_start_web" "$@"
